spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/freelance
    username: postgres
    password: Mujammil@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
